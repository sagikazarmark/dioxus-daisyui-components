dioxus_registry_preview::component! {
    group: "overlays",
    examples: {
        overview {
            description: "A provider, and the buttons that send it something to show.",
        },
        types {
            title: "Kinds",
            description: "Every value of the colour axis: the one nobody passes, since a toast's kind is decided where it is dispatched.",
        },
        position {
            description: "Both position axes, reached one at a time: a region is pinned to the viewport, so nine at once would be nine stacks in the corners.",
        },
        appearance {
            title: "Parts of your own",
            description: "The utilities this component emits where daisyUI has no class, switched off, and the toast a caller renders in their place.",
        },
    },
}
