{
  "name": "preview",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i style.css -o assets/style.css --minify",
    "watch": "tailwindcss -i style.css -o assets/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3",
    "daisyui": "^5.7",
    "tailwindcss": "^4.3",
    "wrangler": "^4"
  }
}
